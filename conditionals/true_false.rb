boolean = [true, false].sample

boolean ? puts("I'm true!") : puts("I'm false!")
# The ternary operator is most useful when there are simple 
#conditions with only two possible outcomes. It essentially reads
# like this: <condition> ? <result if true> : <result if false>

#if boolean
 # puts "I'm true!"
#else
 # puts "I'm false"
#end